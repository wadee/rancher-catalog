.catalog:
  name: fluentd
  version: "prod_kafka"
  description: |
    fluentd
  questions:
    - variable: elasticsearch
      label: "elasticsearch master"
      type: "service"
      required: true
      description: |
        elasticsearch master
  services:
    fluent-es:
      scala: 1